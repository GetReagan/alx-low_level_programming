#include "main.h"
/**
* mul - this function multiply two digits
* @a: the first character to be process
* @b: the second character
* Description: this function multiply two numbers?
* Return: return the multyply of two numbers
*/
int mul(int a, int b)
{
	return (a * b);
}
